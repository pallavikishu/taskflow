spring.application.name=taskflow

# MongoDB connection - override via environment variable in production
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/taskflow}

# JWT config - override secret via environment variable in production
app.jwt.secret=${JWT_SECRET:c2VjcmV0LWtleS1mb3ItdGFza2Zsb3ctYXBwLWNoYW5nZS1pbi1wcm9kdWN0aW9uLW1ha2UtbG9uZ2Vy}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

server.port=${PORT:8080}

# CORS - frontend origin
app.cors.allowed-origin=${FRONTEND_ORIGIN:http://localhost:5173}

logging.level.org.springframework.security=INFO
